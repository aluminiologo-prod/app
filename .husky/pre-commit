#!/bin/sh

# 1. Expo doctor — validates SDK compatibility and project health
npx expo-doctor

# 2. Run lint-staged (ESLint on staged .ts/.tsx files)
npx lint-staged

# 3. TypeScript type check (full project, no emit)
npx tsc --noEmit

# 4. Unit tests
npm test
